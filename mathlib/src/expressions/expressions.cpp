#include "expressions/expressions.hpp"

#include "expressionCast.hpp"

namespace cas::math {
    // template<typename TLeft, typename TRight>
    // Expression* add(TLeft left, TRight right) {
    //     return new Addition(toExpression(left), toExpression(right));
    // }

    // template<typename TLeft, typename TRight>
    // Expression* subtract(TLeft left, TRight right) {
    //     return new Addition(left, multiply(-1, right));
    // }

    // template<typename TLeft, typename TRight>
    // Expression* multiply(TLeft left, TRight right) {
    //     return new Multiplication(toExpression(left), toExpression(right));
    // }

    // template<typename TLeft, typename TRight>
    // Expression* divide(TLeft left, TRight right) {
    //     return new Multiplication(toExpression(left), new Exponentiation(toExpression(right), toExpression(-1)));
    // }

    // template<typename TLeft, typename TRight>
    // Expression* power(TLeft left, TRight right) {
    //     return new Exponentiation(toExpression(left), toExpression(right));
    // }

    // instantiateOperations(Expression);

    // template Expression* divide<int, Expression*>(int, Expression*);
} // namespace cas::math
